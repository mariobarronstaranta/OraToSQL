IF NOT EXISTS
(
    SELECT 1
    FROM sys.indexes
    WHERE object_id = OBJECT_ID(N'[EAI].[ENCUESTAS_CLIENTES_INVALIDAS]')
      AND name = N'ENCUESTAS_INVALIDAS_INDEX1'
)
BEGIN
    CREATE NONCLUSTERED INDEX [ENCUESTAS_INVALIDAS_INDEX1]
    ON [EAI].[ENCUESTAS_CLIENTES_INVALIDAS]
    (
    [TERRITORIO] ASC,
    [ANIO_SEMANA] ASC,
    [DIA_SEMANA] ASC,
    [CONSECUTIVO] ASC
    )
    ON [PRIMARY];
END;
GO
